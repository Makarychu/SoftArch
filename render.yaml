services:
  # 🎬 Video Service
  - type: web
    name: video-service
    env: docker
    rootDir: ./services/video_service
    plan: free
    autoDeploy: true
    envVars:
      - key: MONGO_URI
        value: mongodb+srv://<USERNAME>:<PASSWORD>@cluster0.mongodb.net/?retryWrites=true&w=majority
      - key: DB_NAME
        value: videosdb

  # ⚙️ Transcoder Service
  - type: web
    name: transcoder-service
    env: docker
    rootDir: ./services/transcoder_service
    plan: free
    autoDeploy: true
    envVars:
      - key: VIDEO_SERVICE_URL
        value: https://video-service.onrender.com  # 👈 вручную подставь URL после деплоя
      - key: MONGO_URI
        value: mongodb+srv://<USERNAME>:<PASSWORD>@cluster0.mongodb.net/?retryWrites=true&w=majority
      - key: DB_NAME
        value: videosdb

  # 🛡️ Moderation Service
  - type: web
    name: moderation-service
    env: docker
    rootDir: ./services/moderation_service
    plan: free
    autoDeploy: true
    envVars:
      - key: VIDEO_SERVICE_URL
        value: https://video-service.onrender.com  # 👈 тоже вручную
